---
import BlogEntry from "./BlogEntry.astro";

const {collection} = Astro.props
---

<div class="flex flex-col gap-5 min-h-screen">
    {
        collection.reverse().map((entry: any) => (
                <BlogEntry entry={entry}/>
        ))
    }
</div>